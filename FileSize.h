#ifndef FILESIZE_H
#define FILESIZE_H
#include "mainheader.h"
#include "verbalsaint.h"


VS

using namespace std;

struct FileSize{
  size_t size;
  int level;
  string name;
};

VS_END
#endif // FILESIZE_H
